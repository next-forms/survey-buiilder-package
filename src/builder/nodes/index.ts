import { SectionNodeDefinition } from './SectionNodeDefinition';
import type { NodeDefinition } from '../../types';

// Standard node definitions
export const StandardNodes: NodeDefinition[] = [
  SectionNodeDefinition,
];

// Export individual node definitions
export {
  SectionNodeDefinition,
};
